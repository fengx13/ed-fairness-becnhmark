# FairED dependencies (Python 3.10-3.13; the paper used Python 3.13).
# For an editable install with extras, prefer:  pip install -e .[all]

# --- Core: dataset building, modeling, fairness evaluation ---
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
xgboost>=2.0
fairlearn>=0.10
matplotlib>=3.7
seaborn>=0.13
pyyaml>=6.0
tqdm>=4.64
jupyter>=1.0

# --- Bias mitigation (notebooks 03-05) ---
aif360>=0.6
imbalanced-learn>=0.11
BlackBoxAuditing>=0.1.54   # backs aif360's Disparate Impact Remover

# --- Deep models / Adversarial Debiasing (optional, heavy) ---
# tensorflow>=2.16
